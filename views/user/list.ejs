<h1>Users List</h1>
<ul>
  <% users.forEach(user => { %>
    <li><%= user.name %> - <%= user.email %> - Company: <%= user.company ? user.company.name : 'No company' %></li>
  <% }) %>
</ul>
<a href="/users/create">Create New User</a>
